/**
 * `/boards/{id}` — the board's primary tab, no item drawer open.
 *
 * Route marker only: `BoardRouteView`, mounted once by `layout.tsx`, reads
 * this URL via `BoardRouteContext` and renders the actual board.
 */
export default function BoardPage() {
  return null;
}
